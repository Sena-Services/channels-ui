{
  "name": "channels-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "rm -rf dist && vite build && rm -rf ../senaBench/apps/sena_agents_backend/sena_agents_backend/public/channels/assets && cp -r dist/* ../senaBench/apps/sena_agents_backend/sena_agents_backend/public/channels/",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "vite": "^7.3.1"
  }
}
